<p id="notice"><%= notice %></p>

<h1><%= @theme.name %></h1>
<p><%= @theme.qty_items%></p>

<%# @theme.name.each do |theme| %>
<!--  <h2><%#= link_to theme.name, theme %></h2>-->
<%# end %>

<!--<p id="notice"><%#= notice %></p>-->

<%= link_to 'Edit', edit_theme_path(@theme) %> |
<%= link_to 'Back', themes_path %>
<%= link_to 'Destroy', theme_path(@theme), method: :delete, data: { confirm: 'Are you sure?' } %>
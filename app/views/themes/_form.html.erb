<%= form_for(theme) do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :qty_items %>
    <%= f.number_field :qty_items %>
  </div>
  <div class="actions">
    <%= f.submit 'Save' %>
  </div>
<% end %>
